#ifndef LOCUS_BUFFERMANAGER_H
#define LOCUS_BUFFERMANAGER_H

#include <unordered_map>
#include "Buffer.h"

//namespace {
//    class BufferManager {
//    public:
//        BufferManager() = default;
//
//        void add(const int &socketID);
//        void erase(const int &socketID);
//        Buffer* getBufferIn(const int &socketID);
//        bool push_back(const int &socketID,const std::vector<std::byte> &bytes);
//
//    private:
//        std::unordered_map<int, Buffer> buffersMap;
//        size_t buffersSize = 1000;
//    };
//}

#endif //LOCUS_BUFFERMANAGER_H
